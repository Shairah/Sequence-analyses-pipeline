# Sequence-analyses-pipeline

#This repo will document the workflow of sequence analyses pipeline (command), input and output file (only the important files will be saved) involved in amplicon-based sequencing 
